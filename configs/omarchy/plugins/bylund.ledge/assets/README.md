Images used by the README.

- `demo.gif` — the whole loop: drop on the bar icon, switch folders, drag out

The still of the open card lives in the repository root as `preview.png`, which
is where the marketplace looks for a preview image. It is used by the README
from there rather than copied in here, so there is only ever one of it.
