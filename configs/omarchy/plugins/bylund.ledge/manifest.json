{
  "schemaVersion": 1,
  "id": "bylund.ledge",
  "name": "Ledge",
  "version": "0.1.0",
  "author": "Andreas Bylund",
  "license": "MIT",
  "description": "A drop zone on the bar: drop files on it from anywhere, then drag them back out into any window later.",
  "homepage": "https://github.com/andreas-bylund/omarchy-ledge",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Ledge",
    "description": "A drop zone that hangs off the bar. Drop files on it from a file manager, a browser or the terminal, keep them there across workspaces, and drag them back out into whichever window needs them.",
    "category": "Files",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "autoClose": true,
      "allowMove": false,
      "autoCloseSeconds": 3
    },
    "schema": [
      {
        "key": "autoClose",
        "type": "boolean",
        "label": "Close the ledge when the pointer leaves it",
        "defaultValue": true,
        "description": "Files can be dropped straight on the bar icon, so the card does not have to stay open to catch them. Off keeps it up until you close it."
      },
      {
        "key": "allowMove",
        "type": "boolean",
        "label": "Let the target application move files instead of copying",
        "defaultValue": false,
        "description": "Off, a drag out of the ledge is always a copy and the original stays where it is. On, the application you drop into decides, which is the only way it can be a move — Ledge hands over paths and never deletes a file itself. After a drag out, any file that is no longer at its path was moved by whoever took it, and its chip leaves the ledge."
      },
      {
        "key": "autoCloseSeconds",
        "type": "integer",
        "label": "Seconds before it closes",
        "min": 1,
        "max": 15,
        "step": 1,
        "defaultValue": 3,
        "description": "Counted from the moment the pointer leaves both the card and the bar icon. A drag out of the ledge holds it open until the file lands."
      }
    ]
  }
}
